# TODO

- [ ] Rename `dk.easv` package to `dk.sea` across the entire project (package declarations, imports, directory structure, FXML controller references, Maven groupId, .iml files)
- [ ] Live tournament system — teacher-controlled tournament mode where:
  - Teacher starts matchups (single game or best-of-N in parallel)
  - Games run in parallel processes and display live on a shared screen
  - Spectator view similar to the stats window mini-board cards, showing all active games updating in real time
  - Tournament bracket / leaderboard tracking results across rounds
